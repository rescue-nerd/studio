{
  "extends": "./tsconfig.json", // Ensure it extends the base tsconfig
  "compilerOptions": {
    "noEmit": true // Typically, dev config doesn't emit files
  },
  "include": [
    "src", // Include source files for linting and type checking
    ".eslintrc.js"
  ]
}
